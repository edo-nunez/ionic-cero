import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'edo.nunez.cl',
  appName: 'Mi primera aplicación',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
